# Core dependencies
psycopg[binary]==3.2.3
requests==2.32.3
beautifulsoup4==4.12.3
lxml==5.3.0
schedule==1.2.2
python-dotenv==1.0.1
flask==3.0.0
setuptools==75.6.0
pytz==2024.1

# Supabase client (using stable version)
supabase==2.0.2

# Cloudflare R2 (S3-compatible)
boto3==1.35.0

# Google Cloud Storage
google-cloud-storage==2.10.0

# Image processing and compression
Pillow==10.4.0

# Optional OCR dependencies (uncomment if needed)
pytesseract==0.3.13
opencv-python==4.10.0.84
numpy==2.1.2


# Gmail API (optional fallback to send email without SMTP)
google-api-python-client==2.149.0
google-auth==2.35.0
google-auth-oauthlib==1.2.1

